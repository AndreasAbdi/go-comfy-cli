version: 1
aliases:
  audio:
    selector: '.nodes[] | select(.type == "LoadAudio") | .widgets_values[0]'
    type: string
    cardinality: one
  prompt:
    selector: '.nodes[] | select(.title == "Input Text (Prompt)") | .widgets_values[0]'
    type: string
    cardinality: one
  duration:
    selector: '.nodes[] | select(.title == "Float (Duration)") | .widgets_values[0]'
    type: number
    cardinality: one
